services:
  vm:
    build: ./vm
    ports:
      - "3001:3001"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - ./vm/.env
    mem_limit: 4g
    restart: unless-stopped
